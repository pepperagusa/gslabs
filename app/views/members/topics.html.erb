<h3><%= @this_user['name']%></h3>
<a href=<%= @this_user['at_sfn'] %> target=_blank><img src= <%= @this_user['avatar_url_medium'] %>><h5>(full profile)</a></h5>

<br />

<% if @topics.size > 0 then %>
    <table class="table table-striped table-bordered table-condensed">
    <thead>
      <tr>
        <th>Type</th>
        <th>Subject</th>
        <th>Replies</th>
        <th>Me toos</th>
      </tr>
    </thead>

    <tbody>
        <% @topics.each { |topic| %>
          <tr>
            <td><img src=/assets/<%= topic['style'] %>.png></td>
            <td><a href=<%= topic['at_sfn'] %> target=_blank><%= topic['subject'] %></a></td>
            <td><%= topic['reply_count'] %></td>
            <td><%= topic['me_too_count'] %></td>
          </tr>
        <% } %>
    </tbody>
<% else %>
    <h4>No topics</h4>
<% end %>
</table>
